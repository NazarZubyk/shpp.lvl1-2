{
  "name": "httpserver",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./dist && tsc",
    "start": "node dist/index.js",
    "myRunServers": "rm -rf ./dist/servers && tsc && node dist/servers/index.js",
    "myRunClients": "rm -rf ./dist/clients && tsc && node dist/clients/requests.js"
  },
  "author": "Nazar Zubyk",
  "license": "ISC",
  "description": "test server",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.6.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
